# CI/CD for Static file

name: Deploy Static Site to Cloudflare Pages

on:
  push:
    branches:
      - main # This triggers the deployment whenever you push to the 'main' branch

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Pages
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v4
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy . --project-name=fronthyre

# CI/CD for react and next app

# name: Deploy App to Cloudflare Pages

# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Node.js
#         uses: actions/setup-node@v4
#         with:
#           node-version: '20'
#           cache: 'npm' # or 'yarn' or 'pnpm'

#       - name: Install dependencies
#         run: npm ci # or npm install

#       - name: Build project
#         run: npm run build

#       - name: Deploy to Cloudflare Pages
#         uses: cloudflare/wrangler-action@v4
#         with:
#           apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
#           accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
#           # Notice we changed the directory from '.' to './dist' (or './out' for Next.js static exports)
#           command: pages deploy ./dist --project-name=fronthyre
